[project]
name = "flow-pdg"
version = "0.0.1"
description = "Velocity-first redesign of PDG for flow-matching backbones (SD3, FLUX)"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
